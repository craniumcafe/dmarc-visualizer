apiVersion: 1

datasources:
  - name: dmarc-ag
    type: elasticsearch
    access: proxy
    url: https://${opensearch_endpoint}
    editable: false
    jsonData:
      index: dmarc_aggregate-*
      esVersion: 7.10
      timeField: date_range
      sigV4Auth: true
      sigV4Region: ${aws_region}